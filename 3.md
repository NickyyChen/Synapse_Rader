# WyRadar 关键设计决策

---

## 决策 1：多智能体 vs 单智能体 vs 无 Agent

### 业务场景

歪麦 AI Lab 5-8 人，决策者需要从海量 AI 信息中识别值得跟进的技术，输出辅助决策报告。系统需要完成采集、筛选、分析、报告、推送五个性质不同的任务，且 4 周内 1-2 人落地 MVP。

### 假设

题目要求"智能化系统"，暗示 LLM 参与语义判断；要求"持续扫描"，暗示自动化采集；要求 4 周 MVP，暗示架构不能过度复杂。采集和分析是 I/O 密集和 LLM 密集两类不同的负载，需要独立的资源调度。AI 信源的 API 天然支持并行调用。系统上线后日报质量可能波动，需要能快速定位是哪个环节出了问题。

### 思考

五个任务性质差异太大——采集纯 I/O、策展规则+语义、分析重推理、报告轻生成、分发纯 API。单 Agent 把五套指令塞进一个 Prompt，当日报质量异常时无法定位是哪个环节出错——假设要求"快速定位问题"，单 Agent 无状态边界，做不到。LangGraph 的显式 AgentState 让每步输入输出独立快照，查日志直接定位故障节点。假设中"AI 信源 API 支持并行"和"两类负载需独立调度"指向并行是刚需——LangGraph Send API 原生支持 4 信源并行采集和 15 条并行分析，CrewAI 线性模型、AutoGen 对话驱动都不适合确定性日报流水线。

### 取舍

用 LangGraph 而非单 Agent：接受 2-3 天学习曲线（StateGraph/Node/Edge/Checkpointer），换来确定性的状态追踪和并行能力。用 LangGraph 而非 CrewAI：CrewAI 的角色层级模型适合多团队协作场景，不支持并行 Send——而我们需要 4 信源并行采集 + 15 条情报并行分析。用 LangGraph 而非 AutoGen：AutoGen 的对话协商适合代码生成场景，对话不确定性对固定日报流程是负担。用 Agent 而非无 Agent：传统脚本流水线无法做 LLM 驱动的语义判断——这是系统核心价值所在。

---

## 决策 2：评价标准由系统预设，不由用户自定义

### 业务场景

歪麦决策者需要判断"新模型/框架/路线值不值得投入资源跟进"。题目要求"评测其工程价值能力边界"，但未定义评测标准由谁制定。系统 4 周 MVP，5-8 人团队中存在技术决策者（偏商业）和技术研究员（偏技术）两类角色。

### 假设

决策者的判断标准是隐性的——"看到好的就知道了"，无法直接外化为参数。5-8 人团队中两类角色对"值得关注"的定义不同。4 周 MVP 不允许开发配置界面+调参反馈系统。决策者会每天阅读日报，系统可以在上线后收集反馈数据来校准标准。题目要求系统"辅助决策"而非"替代决策"——系统是建议者，最终判断权在人。

### 思考

假设明确决策者的标准是隐性的、两类角色标准冲突、4 周装不下配置界面。如果让用户自定义权重：用户说不清标准，调滑条是假掌控；一套权重无法同时服务技术决策者（要商业）和技术研究员（要技术）；配置界面至少多 1 周。因此选择系统预设——四维直接映射决策逻辑（能赚钱吗/能用吗/好用吗/适合我们吗），权重向商业倾斜优先服务决策者。置信度标注替代权重调参——"商业潜力 5（置信度 90%）vs 5（置信度 50%）"比让用户纠结 0.45 还是 0.5 更实用。这落地题目"辅助"二字：系统给有把握度的建议，人不依赖系统做最终判断。

### 取舍

用系统预设而非用户自定义：接受设计者偏见风险（预设权重可能与决策者真实偏好偏差 >30%），换 4 周 MVP 可行性和置信度标注的诚实性。缓解措施：上线第 1 周人工校准（对比决策者实际跟进 vs 系统推荐，准确率 <70% 则调整权重）。P1 引入角色分版（决策版/研究版两套预设而非自由调参），P2 加入反馈按钮收集偏好数据。不用 LLM 自行决定标准：同一情报昨天和今天的评分可能不同，结果不可复现。

---

## 决策 3：RAG 集成

### 业务场景

歪麦团队痛点之一是"历史遗忘"——上周看过一个有潜力的框架，这周又出一个类似的，记不清区别。题目要求"持续扫描"和"跟踪"，暗示系统需要跨时间维度的对比能力。决策者的问题"这个新东西值不值得跟"隐含了"和已有的相比怎么样"的对比需求。

### 假设

AI 领域的新情报与历史情报存在语义关联（同一框架的版本迭代、同一赛道的不同方案、同一事件的多源报道）。历史分析结果具有复用价值——今天分析的结论可能成为明天分析的参照。同批次处理的情报之间可能存在相关性（同一天 GitHub 上可能同时出现同一项目的多个相关信息）。ChromaDB 嵌入式模式在日均 15-30 条写入量下稳定运行。

### 思考

业务场景中团队痛点是"历史遗忘"——上周看过的框架这周记不清。假设明确 AI 情报存在语义关联且历史分析可复用。无 RAG 时每条情报独立分析：同一框架的 2.0 和 1.0 被当作两个条目各自打高分，"本月第 4 个多 Agent 框架"这类洞察丢失。因此选择在 Analyst 内部集成 RAG——分析前检索 top-5 历史情报注入 Prompt，让 LLM 带着历史记忆做判断。放内部而非独立节点：检索和分析强耦合，拆开只多一层序列化。即时索引（分析完一条立刻写入 ChromaDB）：假设中同批次情报存在相关性，第 15 条能检索到刚分析完的第 1 条，实现同批次互检。

### 取舍

用 RAG 而非无 RAG：接受 ChromaDB 运维复杂度（但嵌入式模式零部署）和 Analyst 代码复杂度（分析函数内多了检索→注入→写入三步），换取历史去重、趋势对比、热度追踪、关联发现四种跨时间维度能力。不用独立 RAG 节点：保持 5 节点拓扑简洁，减少 AgentState 序列化开销。不用外部向量数据库（Qdrant/Milvus）：ChromaDB 嵌入式与 SQLite 同理，零外部依赖，日均几十条数据完全够用。

---

## 决策 4：四维评分体系——维度选择、自适应权重、置信度

### 业务场景

歪麦决策者面对一条情报时的判断可拆为四个问题：有没有商业需求（商业潜力）？代码能跑吗（落地难度）？比现有方案强多少（性能指标）？跟公司方向匹配吗（业务兼容性）？题目要求"评测其工程价值能力边界"，工程价值是多维度的——能用的才谈得上价值，能赚钱的才值得投入。

### 假设

决策者的判断逻辑可以映射为可操作的评分维度。每个维度都能基于公开信息（README/论文摘要/API 数据）被 LLM 可靠评分。不同情报类型（开源工具/论文/产品动态）的评分侧重点不同——统一权重会歪曲判断。LLM 的评分不是绝对可靠的——需要诚实标注"把握多大"。系统第一优先级是服务技术决策者（资源分配权），商业价值是他们的首要关注点。

### 思考

决策者判断逻辑天然拆为四个问题——能赚钱吗/能用吗/好用吗/适合我们吗。假设明确每维可基于公开信息评分、决策者优先、不同情报类型侧重点不同、LLM 评分不可靠。因此四维映射判断框架，商业潜力权重最高（0.45）——决策者管资源分配，核心看投入产出比。删维度丢关键角度（去掉落地难度：无法部署的工具评分无意义），加维度超 LLM 评分边界（团队能力匹配——LLM 不知团队技术栈）。上下文自适应解决统一权重偏差——开源工具提落地权重，论文提性能权重，产品动态拉满商业权重。置信度标注应对 LLM 不可靠——低置信度标 ⚠️，落地"辅助"二字。

### 取舍

用四维而非六维/八维：接受可能遗漏"许可证风险""学术影响力"等维度，换 LLM 评分的一致性和可操作性。用自适应权重而非固定权重：接受 LLM 需先判断情报类型（准确率 ≥80%），换取不同场景下的评分合理性。用置信度标注而非隐藏不确定性：接受报告中出现 ⚠️ 标记，换取决策者对系统判断的准确校准。不用二元判断（"跟/不跟"）：保留评分理由和子维度明细，决策者需要知道"为什么跟、风险在哪"，而非只要结论。

---

## 决策 5：主力 LLM 模型选择与分级策略

### 业务场景

系统每日约 18 次 LLM 调用，分布在三个认知层次——Curator 分类去噪（简单二分类/多分类，35+20 条批量）、Analyst 摘要评分（重推理，15 条并行）、Editor 润色（语言转换，1 次）。题目要求 4 周 MVP、1-2 人团队，模型选型需同时满足"中文能力强"（处理中文信源）、"成本可控"（日均费用不成为负担）、"API 稳定"（不阻塞日报生成）。

### 假设

Analyst 评分是系统价值中枢——评分不准则日报无用，此节点需要最强的推理模型。Curator 分类和 Editor 润色是认知复杂度较低的确定性任务，不需要最高等级的推理能力。DeepSeek API 兼容 OpenAI 格式，切换模型仅改 model 参数，零代码改动。国产模型对中文技术文本的理解优于海外模型。1-2 人团队 4 周内没有余力维护本地模型部署。

### 思考

系统每日 18 次 LLM 调用分三个认知层次——Curator 简单分类、Analyst 重推理、Editor 语言润色。假设明确 Analyst 是价值中枢（评分不准则日报无用），Curator/Editor 是低复杂度任务。全用 v4-pro 是浪费——Curator 对 35 条做二分类、Editor 给 Markdown 加过渡语根本不需要 v4-pro 的推理深度，还会挤占速率配额。因此 Analyst 保留 v4-pro（不可替代），Curator/Editor 降级 v3。假设明确 DeepSeek API 兼容 OpenAI 格式、切换模型零改动——降级无风险。进一步优化：假设约 40-50% 情报总分 <3.0，用 v3 快速初评筛掉低分条目，仅 ≥3.0 调 v4-pro 完整分析，调用量减半，释放配额支撑信源扩展。

### 取舍

用 DeepSeek-v4-pro 而非 Claude/GPT-4o：后者中文技术文本理解不如国产模型，且成本高一个数量级，日均费用从 ¥0.3 变为 ¥3+。用 v4-pro 而不用本地部署模型（Qwen2.5-72B 等）：本地部署需 GPU 服务器（A100 级别）和模型运维，1-2 人 4 周无余力，硬件成本远超 API 费用。用分级策略而非全 v4-pro：Curator L2/L3/Editor 降级为 v3，v4-pro 调用量从 18 次降到约 12 次（降级后）再降到 7-10 次（加预筛选后），释放速率限制配额支撑信源扩展。不用单一模型全家桶：全链路绑定一个模型是单点故障风险——v4-pro 服务中断则整个日报生成失败，多模型策略也是容灾策略。
