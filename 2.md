# WyRadar 方案设计 — 架构图与关键模块

## 1. 系统全景架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            WyRadar 技术情报雷达                               │
│                     LangGraph 多智能体系统 · 日均处理 15-30 条情报              │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────────┐│
│  │                      APScheduler (内嵌 FastAPI)                          ││
│  │                    Cron: 0 7 * * * (采集) / 0 8 * * * (推送)             ││
│  │                    POST /api/trigger/daily-run (手动)                    ││
│  └────────────────────────────────┬────────────────────────────────────────┘│
│                                   │ invoke()                                  │
│  ┌────────────────────────────────▼────────────────────────────────────────┐│
│  │                     LangGraph StateGraph 编排引擎                         ││
│  │                                                                          ││
│  │  ┌──────────────────────────────────────────────────────────────────┐   ││
│  │  │                        AgentState (共享状态)                       │   ││
│  │  │  run_id · trigger · current_stage · error · status               │   ││
│  │  │  raw_items[] · curated_items[] · analyses[] · report_markdown    │   ││
│  │  └──────────────────────────────────────────────────────────────────┘   ││
│  │                                                                          ││
│  │  ┌──────────────┐   ┌──────────────┐   ┌──────────────────────────┐    ││
│  │  │  Node 1      │   │  Node 2      │   │  Node 3                   │    ││
│  │  │  Collector   │   │  Curator     │   │  Analyst                  │    ││
│  │  │  ─────────── │   │  ─────────── │   │  ──────────────────────── │    ││
│  │  │  职责: 采集   │   │  职责: 筛选   │   │  职责: 摘要+评分            │    ││
│  │  │              │   │              │   │                           │    ││
│  │  │ ┌──────────┐ │   │ L1 关键词过滤 │   │ ┌─────────────────────┐  │    ││
│  │  │ │ GitHub   │ │   │ (规则引擎)    │   │ │ Step 1: RAG 检索    │  │    ││
│  │  │ │ Trending │ │   │    ↓         │   │ │ ChromaDB 混合检索    │  │    ││
│  │  │ ├──────────┤ │   │ L2 LLM 分类  │   │ │ (向量0.7+BM25 0.3)  │  │    ││
│  │  │ │ Hugging- │ │   │ (DeepSeek)   │   │ │ top-5 历史情报       │  │    ││
│  │  │ │ Face     │ │   │    ↓         │   │ └──────────┬──────────┘  │    ││
│  │  │ ├──────────┤ │   │ L3 LLM 去噪  │   │            ↓             │    ││
│  │  │ │ Model-   │ │   │ (DeepSeek)   │   │ ┌─────────────────────┐  │    ││
│  │  │ │ Scope    │ │   │              │   │ │ Step 2: LLM 分析     │  │    ││
│  │  │ ├──────────┤ │   │ 输入: ~60条   │   │ │ 摘要 + 4维评分       │  │    ││
│  │  │ │ arXiv    │ │   │ 输出: ~15条   │   │ │ + 置信度 + 对比      │  │    ││
│  │  │ └──────────┘ │   │              │   │ │ (DeepSeek, t=0.2)   │  │    ││
│  │  │              │   │              │   │ └──────────┬──────────┘  │    ││
│  │  │ 并行: 4线程   │   │ 批量LLM调用   │   │            ↓             │    ││
│  │  │ 容错: 单源失败 │   │ 可配置关键词  │   │ ┌─────────────────────┐  │    ││
│  │  │      不阻塞   │   │              │   │ │ Step 3: 写入ChromaDB │  │    ││
│  │  │              │   │              │   │ │ (即时索引)           │  │    ││
│  │  │ 输出→raw_items│   │ 输出→curated │   │ └─────────────────────┘  │    ││
│  │  └──────┬───────┘   └──────┬───────┘   │                           │    ││
│  │         │                  │           │ 并行: 8线程               │    ││
│  │         │                  │           │ 输出→analyzed_items       │    ││
│  │         └──────────────────┼───────────┘                           │    ││
│  │                            │                                        │    ││
│  │  ┌─────────────────────────┼────────────────────────────────────┐  │    ││
│  │  │                         ▼                                     │  │    ││
│  │  │  ┌──────────────┐                          ┌──────────────┐  │  │    ││
│  │  │  │  Node 4      │                          │  Node 5      │  │  │    ││
│  │  │  │  Editor      │─────────────────────────→│  Dispatcher  │  │  │    ││
│  │  │  │  ─────────── │                          │  ─────────── │  │  │    ││
│  │  │  │  职责: 日报生成│                          │  职责: 多渠道推送│  │  │    ││
│  │  │  │              │                          │              │  │  │    ││
│  │  │  │ 1.按评分分组  │                          │ 飞书Bot群消息  │  │  │    ││
│  │  │  │ 2.LLM润色文案 │                          │ · 概览+强推   │  │  │    ││
│  │  │  │ 3.拼接Markdown│                          │ · ≤10KB      │  │  │    ││
│  │  │  │ 4.写入DB      │                          │ · @all       │  │  │    ││
│  │  │  │              │                          │              │  │  │    ││
│  │  │  │ 输出→daily_  │                          │ 飞书文档      │  │  │    ││
│  │  │  │ reports      │                          │ · 完整日报MD  │  │  │    ││
│  │  │  └──────────────┘                          └──────────────┘  │  │    ││
│  │  └──────────────────────────────────────────────────────────────┘  │    ││
│  └────────────────────────────────────────────────────────────────────┘    ││
│                                                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                          数据持久层                                     │  │
│  │                                                                        │  │
│  │  ┌─────────────────────────┐  ┌──────────────────────────────┐       │  │
│  │  │  SQLite (关系数据)       │  │  ChromaDB (向量数据)          │       │  │
│  │  │  /data/wyradar.db       │  │  /data/chroma/               │       │  │
│  │  │                         │  │                              │       │  │
│  │  │  raw_items              │  │  analyzed_items 向量化索引    │       │  │
│  │  │  analyzed_items         │  │  · text-embedding-3-small    │       │  │
│  │  │  daily_reports          │  │  · 混合检索(向量+BM25)        │       │  │
│  │  │  execution_logs         │  │  · 即时写入(每条分析后)       │       │  │
│  │  └─────────────────────────┘  └──────────────────────────────┘       │  │
│  │                                                                        │  │
│  │  SQLAlchemy 2.x async → 一行配置切换 PostgreSQL                       │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                          交互层                                        │  │
│  │                                                                        │  │
│  │  ┌──────────────────────┐         ┌──────────────────────────────┐    │  │
│  │  │  Vue 3 + Vite SPA    │  fetch  │  FastAPI :8000               │    │  │
│  │  │  ─────────────────── │←────────│  ─────────────────────────── │    │  │
│  │  │  · DailyReport.vue   │  REST   │  GET /api/report/today       │    │  │
│  │  │  · HistorySearch.vue │  API    │  GET /api/report/{date}      │    │  │
│  │  │  · TriggerRun.vue    │         │  GET /api/items (检索)       │    │  │
│  │  │                      │         │  POST /api/trigger/daily-run │    │  │
│  │  │  Naive UI 组件库     │         │  GET /api/run-status/{id}    │    │  │
│  │  └──────────────────────┘         │  GET /api/stats              │    │  │
│  │                                   └──────────────────────────────┘    │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                          外部触达                                       │  │
│  │                                                                        │  │
│  │  ┌──────────────────────┐    ┌──────────────────────┐                 │  │
│  │  │  飞书 Bot 群消息      │    │  飞书文档             │                 │  │
│  │  │  每日 8:00 @all      │    │  完整日报 Markdown    │                 │  │
│  │  │  概览 + 强推摘要      │    │  lark-oapi SDK       │                 │  │
│  │  └──────────────────────┘    └──────────────────────┘                 │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  数据流概要:                                                                 │
│  信源API → Collector(并行4源) → Curator(3级过滤) → Analyst(RAG+评分,并行N条) │
│  → Editor(LLM润色+日报组装) → Dispatcher(飞书Bot+文档+Web)                   │
│                                                                              │
│  时间线: 7:00自动采集 → 7:00-7:25处理窗口 → 8:00飞书推送                    │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 2. 关键模块说明

### 2.1 Collector 采集模块

| 维度 | 说明 |
|------|------|
| 定位 | 系统的数据入口，负责每日从 4 个信源并行抓取 AI 情报 |
| 核心逻辑 | `ThreadPoolExecutor(max_workers=4)` 并行调用 4 个 Fetcher，每个返回 15-20 条；SHA256(url+title) 去重；写入 raw_items 表 |
| 信源 | GitHub Trending (daily/weekly, Python)、HuggingFace (Trending models/datasets/spaces)、ModelScope (热门模型/数据集)、arXiv (cs.AI/CL/CV/LG，按日期检索) |
| 容错 | 单源失败不阻塞其他源，失败源记录到 `fetch_errors`，日报标注"今日XX源采集失败" |
| 性能 | 4 源并行总耗时 < 60 秒 |
| 技术 | httpx (async) + feedparser (arXiv RSS) + ThreadPoolExecutor |

### 2.2 Curator 策展模块

| 维度 | 说明 |
|------|------|
| 定位 | 从采集噪声中提取信号，将 60 条原始条目精简为 15 条高质量情报 |
| 核心逻辑 | 三级过滤流水线：L1 关键词匹配(规则引擎,毫秒级,~60→35条) → L2 LLM 分类(DeepSeek t=0.1,~35→20条,打一二级标签) → L3 LLM 去噪(DeepSeek t=0.1,~20→15条,剔除灌水/营销) |
| 关键词列表 | Agent/LLM/Multimodal/NLP/Transformer/RLHF/RAG/MCP/function-calling/tool-use/大模型/多模态/智能体等 18+ 关键词，配置文件可编辑 |
| 标签体系 | 一级: AI Agent / 多模态大模型 / NLP；二级: 新模型发布 / 论文 / 开源工具/框架 / 技术路线/观点 / 数据集 / 产品动态 |
| 淘汰标记 | `filtered_out_keyword`(L1淘汰) / `filtered_out_noise`(L3淘汰)，原因可追溯 |
| 技术 | Python 规则引擎 + DeepSeek API (批量处理，一次调用处理多条) |

### 2.3 Analyst 分析模块（系统核心）

| 维度 | 说明 |
|------|------|
| 定位 | 系统的价值中枢，对每条情报生成结构化分析报告和四维评分 |
| 核心逻辑 | 三步处理：Step1 RAG检索历史top-5相似情报(ChromaDB混合检索) → Step2 LLM生成摘要+四维评分+置信度(DeepSeek t=0.2) → Step3 分析结果即时写入ChromaDB |
| 并行策略 | `ThreadPoolExecutor(max_workers=8)`，15 条情报并行分析，总耗时 ≈ 最慢单条耗时 + 开销 < 3 分钟 |
| 四维评分 | 商业潜力(0.45) / 落地难度(0.20) / 性能指标(0.20) / 业务兼容性(0.15)；权重根据情报类型动态调整(开源工具→落地难度+0.10；论文→性能指标+0.10；产品动态→商业潜力+0.10) |
| 输出字段 | 一句话总结(≤50字) + 3核心亮点 + 技术对比 + 4维评分(含value/confidence/reason三元组) + 综合置信度 + 推荐等级 + RAG引用 |
| 评分机制 | Few-shot锚定(3个标准示例) + 置信度标注(<0.6标记⚠️) + 解释质量区分(数据驱动vs主观判断) |
| 容错 | 单条分析失败不影响其他条，失败条目不阻塞后续流程 |
| 技术 | DeepSeek-v4-pro + ChromaDB(混合检索) + text-embedding-3-small |

### 2.4 RAG 检索引擎

| 维度 | 说明 |
|------|------|
| 定位 | 为 Analyst 提供跨时间维度的对比上下文，解决"历史遗忘"痛点 |
| 核心逻辑 | 新情报 title+description → Embedding 向量化 → ChromaDB 混合检索(向量相似度0.7 + BM25关键词0.3) → 返回top-5相似历史情报 → 注入 LLM Prompt |
| 索引内容 | 每条 analyzed_item 的 title + summary_one_liner + summary_highlights 拼接为检索文本；metadata 含分类/评分/来源/时间/URL |
| 更新时机 | 每条情报分析完成后即时写入，同批次后续条目立即可检索到 |
| 降级策略 | 首次运行时 ChromaDB 为空，返回空列表，不报错，系统正常降级 |
| 能力 | 历史去重(检测重复推荐) + 趋势对比(同类技术演进脉络) + 热度追踪(同一项目star变化) + 关联发现(隐含关联) |
| 技术 | ChromaDB(嵌入式,PersistentClient) + text-embedding-3-small(512维) + BM25Okapi(rank-bm25库) |

### 2.5 Editor 主编模块

| 维度 | 说明 |
|------|------|
| 定位 | 将分散的分析结果整合为结构清晰、重点突出的日报 |
| 核心逻辑 | 读取当日所有 analyzed_items → 按 score_total 降序 → 按推荐等级分组 → LLM润色各组文案(不对评分结果做修改) → 拼接为完整 Markdown → 写入 daily_reports 表 |
| 日报结构 | 5个固定板块：今日概览(统计数字) → 强烈推荐(≥4.0,含评分明细+置信度+RAG对比) → 值得关注(3.0-3.9,同结构精简) → 暂不跟进(2.0-2.9,简表) → 全量列表(完整表格) |
| LLM角色 | 仅做语言润色和衔接过渡(温度0.4,允许表达多样性)，不修改评分结果 |
| 性能 | 生成耗时 < 30 秒 |
| 技术 | DeepSeek-v4-pro (t=0.4) + Jinja2 模板辅助 |

### 2.6 Dispatcher 分发模块

| 维度 | 说明 |
|------|------|
| 定位 | 将日报多渠道触达决策者，确保重要信息不被遗漏 |
| 核心逻辑 | 双渠道并行推送：飞书Bot群消息(概览+强推摘要,≤10KB,@all) + 飞书文档(完整日报MD) |
| 飞书Bot消息 | 格式：概览数字 + 强烈推荐条目(含一句话总结+评分概要+链接) + 飞书文档链接，@all提醒 |
| 飞书文档 | 完整5板块日报，Markdown自动渲染为飞书格式，团队可见权限 |
| LLM依赖 | 无(纯API调用，不需LLM决策) |
| 容错 | 推送失败记录日志，Web页面仍可查看日报；手动触发模式同样推送 |
| 技术 | lark-oapi SDK (im.v1.message.create + docx.v1.document.create) |

### 2.7 FastAPI 后端服务

| 维度 | 说明 |
|------|------|
| 定位 | REST API 服务 + LangGraph 宿主 + 定时调度宿 |
| 端点 | `GET /api/report/today`(今日日报) / `GET /api/report/{date}`(历史日报) / `GET /api/items`(检索,11个查询参数) / `GET /api/stats`(统计) / `POST /api/trigger/daily-run`(手动触发) / `GET /api/run-status/{run_id}`(执行状态轮询) / `GET /api/run-history`(执行记录) |
| 调度 | APScheduler 内嵌：`0 7 * * *`(采集+分析) / `0 8 * * *`(推送)；手动触发与定时任务互斥(DB行锁防并发) |
| 流式 | 手动触发后通过 SSE 推送各节点执行状态到前端 |
| 技术 | FastAPI 0.115+ · Pydantic v2 · uvicorn 0.34+ · APScheduler 3.10+ |

### 2.8 Vue 3 前端 SPA

| 维度 | 说明 |
|------|------|
| 定位 | Web 交互界面，提供日报查看、历史检索、手动触发的完整操作体验 |
| 页面 | DailyReport.vue(今日日报,卡片展开/折叠,筛选,分页) / HistorySearch.vue(多条件检索,11参数API,详情展开) / TriggerRun.vue(手动触发+确认弹窗+实时进度条+执行日志) |
| 组件 | ReportCard(情报卡片) · ScoreBar(评分色块条形图) · FilterBar(筛选栏) · RunStatus(执行进度) · Pagination(分页) |
| 状态管理 | MVP 不引入 Pinia，组件内 reactive/ref 管理状态 |
| 路由 | MVP 不引入 Vue Router，Tab 切换实现页面导航 |
| UI库 | Naive UI (Table/DatePicker/Modal/Progress 组件开箱即用) |
| 技术 | Vue 3.5+ · Vite 5+ · TypeScript 5.5+ · Naive UI 2.40+ |

### 2.9 定时调度引擎

| 维度 | 说明 |
|------|------|
| 定位 | 系统自动化运行的驱动力，无需人工干预 |
| 任务 | 采集分析任务(Cron: 0 7 * * *) / 推送任务(Cron: 0 8 * * *) |
| 分时原因 | 7:00-8:00 为处理窗口(~15-25分钟)；8:00准时推送匹配用户习惯；若7:00任务超时, 8:00推送标记为partial |
| 容错 | 7:00失败→推送告警消息；超时→推送已完成部分；进程重启→调度器自动恢复；手动/定时互斥→DB锁 |
| 技术 | APScheduler 3.10+ (BackgroundScheduler, 内嵌FastAPI进程) |

### 2.10 数据模型关系

```
                         ┌──────────────────┐
                         │   daily_reports   │
                         │  ─────────────── │
                         │  report_date(PK)  │
                         │  report_markdown  │
                         │  feishu_msg_id    │
                         │  feishu_doc_url   │
                         │  execution_time   │
                         └────────┬─────────┘
                                  │ 关联日期
                                  │
  ┌──────────────────┐   ┌───────┴──────────┐   ┌──────────────────┐
  │    raw_items      │   │  analyzed_items   │   │  execution_logs   │
  │  ─────────────── │   │  ─────────────── │   │  ─────────────── │
  │  id(PK)          │──→│  id(PK)          │   │  id(PK)          │
  │  source          │ 1:1│  raw_item_id(FK) │   │  run_id          │
  │  title           │   │  summary_*        │   │  trigger         │
  │  url             │   │  score_*(1-5)     │   │  node_name       │
  │  status          │   │  score_*_confidence│  │  status          │
  │  category_l1/l2  │   │  score_*_reason   │   │  items_processed │
  │  filter_reason   │   │  score_total      │   │  started_at      │
  │  fetched_at      │   │  confidence_overall│  │  finished_at     │
  └──────────────────┘   │  recommend_level  │   └──────────────────┘
                          │  rag_context_used │
                          │  weight_adjustment│
                          └────────┬─────────┘
                                   │ 1:1 向量同步
                                   │
                          ┌────────┴─────────┐
                          │    ChromaDB       │
                          │  ─────────────── │
                          │  id (同analyzed)  │
                          │  document (检索用) │
                          │  embedding (512维)│
                          │  metadata (分类/   │
                          │    评分/来源/时间) │
                          └──────────────────┘
```
