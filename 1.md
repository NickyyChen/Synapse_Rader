# WyRadar 题目需求质疑与假设 — 十个维度

---

## 1. "工程价值"的本体论模糊——一个未被定义的复合概念

**歧义点：** 题目要求"评测其工程价值能力边界"，将"工程价值"和"能力边界"并置，但两个概念是正交而非从属关系。工程价值回答"值不值得用"，能力边界回答"能做什么/不能做什么"。一个技术可能能力边界窄但工程价值极高（如专精某细分任务的工具），也可能能力边界宽但工程价值低（如无法部署的研究代码）。

**我的观点：** 题目将两者捆绑，暗示评测体系应同时覆盖"价值判断"和"边界探测"。但 MVP 阶段资源有限，我们选择**优先工程价值，间接覆盖能力边界**——通过落地难度维度（是否有开源代码、文档完整度、部署成本）和 RAG 历史对比（与同类技术的差异）来侧面反映能力边界，而非直接做能力边界的系统性测试。

**假设：** 决策者最迫切的需求是"这个东西值不值得跟"，而非"这个东西的精确能力边界在哪里"。如果决策者对能力边界的精度要求更高（如需要知道模型在哪些边缘 case 上失效），那当前的落地难度评分不足以替代，需要引入实测验证。

---

## 2. "智能化"的梯度歧义——从规则到自主的光谱

**歧义点：** 题目说需要"智能化系统"，但"智能化"是一个光谱：最低是规则引擎（关键词过滤），中间是 LLM 辅助（摘要+评分），最高是自主 Agent（自行判断采集目标、调整评测标准、甚至主动发起深度调研）。题目没有说明期望的智能化梯度，而这直接决定了系统架构的复杂度。

**我的观点：** 我们选择了**LLM 辅助 + Agent 编排**的中高梯度——LLM 负责需要语义理解的环节（分类/去噪/评分/摘要），Agent（LangGraph）负责流程编排（调度节点、并行执行、容错恢复），但**不让 Agent 自行改变评测标准或采集目标**。这些关键决策权保留在设计者和配置文件中。

**假设：** 题目中的"智能化"不等于"全自主"。一个能够在固定流水线中调用 LLM 做语义判断的系统已经满足"智能化辅助决策"的需求。如果面试官期望的是能自主发现新信源、自定评测维度的系统，那当前设计需要大幅调整。

---

## 3. "持续扫描"的时态歧义——实时流 vs 定时批处理

**歧义点：** "持续扫描"可以被解读为：(a) 24×7 实时监听社区动态，类似舆情监控系统；(b) 定时批量采集，如每日汇总。两者的架构差异巨大——实时流需要事件驱动 + 消息队列 + 流处理引擎；定时批处理只需要 Cron + 批量 API 调用。

**我的观点：** 我们选择每日批量采集（7:00 执行）而非实时流。理由：(1) AI 社区动态的"事件粒度"本身是天级别的——arXiv 每天更新一次，GitHub Trending 每天刷新；(2) 实时流在 4 周 MVP 中不可行——需要 WebSocket/SSE 监听、消息队列、流处理，基础设施复杂度翻倍；(3) 决策者每天早上一份日报的使用模式与批量采集天然匹配。

**假设：** 决策者不需要在论文发布后 1 小时内就收到通知。如果存在高频决策场景（如竞品监控需要小时级响应），需要将实时热点告警（P1 F-13）的优先级提前。

---

## 4. 信息源的边界与可及性——"AI 社区动态"的理想与 API 现实的鸿沟

**歧义点：** 题目说探测"AI 社区动态"，列举了新模型、论文、开源工具、技术路线。但"社区动态"的信息源边界未被定义——只包括有公开 API 的平台（GitHub/HuggingFace/arXiv），还是也包括需要爬虫的社交平台（Twitter/X/Reddit/微信公众号），甚至包括付费情报源（CB Insights/PitchBook）？

**我的观点：** 我们选择 4 个有公开 API 的信源（GitHub/HuggingFace/ModelScope/arXiv）作为 MVP。这不是因为它们是最好的信源，而是**在 4 周内能稳定接入的信源中它们是最好的**。Twitter/X 的 API 在 2023 年后大幅限制免费访问；微信公众号没有公开 API；Reddit API 虽然可用但 AI 相关内容的信噪比极低。这些信源的采集器需要爬虫 + 反爬对抗，远超 MVP 的工程预算。

**假设：** 4 个信源覆盖了 AI 领域约 70% 的重要动态。如果面试官认为缺少社交平台会导致关键情报遗漏（如某重要模型的 Twitter 首发信息），那需要在 P1 中优先扩展社交信源。

---

## 5. "决策者"的同质化假设——隐性的单一思维主体

**歧义点：** 题目始终以单数形式提及"决策者"，但 Lab 团队 5-8 人，且从问题描述看至少存在两类角色：做出资源分配决策的人（偏商业/管理）和做技术深度评估的人（偏技术/工程）。这两类人对"值得关注"的定义几乎不可能完全一致。

**我的观点：** 题目隐含地将"决策者"视为同质化群体，这是一个危险的简化。在 PRD 中我们识别了三个角色（技术决策者/技术研究员/系统管理员），并在评分体系中面临内在张力：商业潜力权重 0.45 优先服务决策者，但技术研究员可能更关注性能指标和落地难度。目前的缓解方案是"全量列表保留所有维度明细，研究员可自行按其他维度排序"。

**假设：** 技术决策者是系统的最终客户，商业潜力权重最高是合理的。如果歪麦公司的决策文化是技术驱动优先（即技术先进性比商业潜力更重要），需要重新分配权重。

---

## 6. "输出作参考"的交互模式缺失——Push vs Pull 的未决问题

**歧义点：** 题目说"输出给内部决策者作参考"，但"输出"的交互模式完全未定义。是被动推送（系统定时把报告送到决策者面前）还是主动查询（决策者有问题时来系统搜索）？还是两者结合？交互模式决定了系统的推送机制、前端设计、甚至数据存储的检索优化方向。

**我的观点：** 我们选择了**Push + Pull 双模式**——每日 8:00 飞书 @all 推送（Push，确保触达），加上 Web 页面的历史检索和手动触发（Pull，满足深度查询和临时需求）。飞书群消息只发概览+强推（≤10KB），完整报告在飞书文档和 Web 页面。这种设计的核心理念是：**用 Push 对抗信息过载（强制触达最重要的 2-3 条），用 Pull 满足信息深度（即时查询历史）。**

**假设：** 决策者愿意每天早上被 @all 提醒。如果团队文化反对高频全员通知，Push 应降级为静默发送（不加 @all）或改为飞书文档更新通知。

---

## 7. "技术路线"的可操作性困境——最难量化的评测对象

**歧义点：** 题目列举了四类资源：新模型、论文、开源工具、技术路线。前三者相对具象——模型有 benchmark、论文有摘要、工具有 README。但**"技术路线"**（如"从 RAG 转向 Agentic RAG"、"从 Prompt Engineering 转向 DSPy 编程"）是抽象趋势，不是单一实体。它不能被"采集"到，只能通过多条具象情报的聚合分析来识别。

**我的观点：** 当前系统对"技术路线"的覆盖最弱。PRD 中的 RAG 历史对比和趋势分析（"这是我们本月看到的第 3 个多 Agent 框架"）是对技术路线的初步探测，但远非系统性识别。真正的技术路线追踪需要：(1) 对历史数据的聚类分析；(2) 识别新兴关键词的频率增长；(3) 跨时间维度的叙事生成。这些都超出了 MVP 范围——我们在 P2 的"趋势周报/月报"（F-15）中规划了初步的时间维度聚合。

**假设：** MVP 阶段做好具象资源的追踪（模型/论文/工具）已经足够有价值，技术路线的识别可以滞后。如果决策者的核心诉求恰好是"我们该跟哪个技术路线"，那 MVP 的功能设计存在盲区。

---

## 8. 4 周 MVP 的人力可行性——1-2 人的实际产出边界

**歧义点：** 题目要求"4 周内由 1-2 个人落地一个可用 MVP"。这是一个硬约束，但它与"智能化系统"的期望之间存在天然张力。一个覆盖采集→筛选→分析→报告→推送→前端的端到端系统，即使是最小化版本，对 1-2 人 4 周来说也非常紧张。

**我的观点：** 4 周可行，但需要严格的 scope discipline。PRD 中的 10 个 P0 功能预估 19-24 天，按 1 人全时（~20 工作日）刚好卡线，2 人则有余量。关键路径上的风险点是：(1) LangGraph 的学习曲线（需 2-3 天）；(2) 评分 Prompt 的调优（需要多轮迭代才能达到可用质量）；(3) 飞书 Bot + 文档的 API 调试（飞书开放平台的文档质量和审核流程不可控）。如果这三点中任何一点超出预期时间，4 周交付会有延期风险。

**假设：** 开发人员至少有 1 人熟悉 Python 异步编程和 LangChain 生态，另 1 人熟悉 Vue 3。如果两人都不熟悉这些技术栈，4 周几乎不可行——需要加上至少 1 周的学习缓冲。

---

## 9. 评测的时间衰减——今天 5 分的项目明天可能不再重要

**歧义点：** 题目没有提到情报价值的**时效性**。AI 领域的"强烈推荐"有半衰期——一个今天评分 4.5 的开源框架，如果 2 周后社区活跃度骤降、维护者停止更新，它的实际推荐价值已经大幅衰减。但系统如果只做一次性评分不做追踪更新，"强烈推荐"标签会随时间推移而失真。

**我的观点：** 我们在 PRD 中引入了 GrandJury 的时间衰减公式（`score_decayed = score × 0.95^(days)`），但这只是数学层面的打折，不是真正的追踪更新。真正解决需要：(1) 对已入库的高分项目做周期性重新采集和重评分；(2) 追踪 GitHub stars/forks/issues 的时间序列变化；(3) 在日报中标注"距上次分析已过 X 天"。这些在 MVP 中均未覆盖。

**假设：** 决策者理解日报中的评分是基于分析时刻的快照判断，不代表持续有效。如果决策者期望评分具有持续性（即查看 2 周前的日报时评分仍然可信），那需要引入周期性重评分机制。

---

## 10. 系统自身的演进路径——"逐步演进"的方向未定义

**歧义点：** 题目说"之后逐步演进"，但演进的方向完全未定义。是横向扩展（增加信源、增加评测维度），还是纵向深化（从 LLM 辅助判断到实测复现验证），还是智能化升级（从固定流水线到自主 Agent）？"演进"的模糊性导致 MVP 的设计需要在可扩展性上预埋接口，但不知道应该预埋什么方向的接口。

**我的观点：** 我们在技术选型中做了"预留升级路径"的设计——SQLAlchemy 兼容 SQLite 和 PostgreSQL、ChromaDB 可迁移到 Qdrant、Vue 3 SPA 可升级到引入 Vue Router + Pinia。但这些都是**基础设施层的平滑升级**，不是**功能层的演进方向**。PRD 中的 17 个功能清单按 P0/P1/P2/P3 排了优先级，但这个优先级是基于我们对"决策者最需要什么"的判断，不是基于歪麦公司的实际业务优先级。

**假设：** 信源扩展和用户偏好定制是最有价值的演进方向。如果歪麦公司实际最需要的是深度复现验证（如评测模型在自身业务数据上的表现），那 P2 的 F-14 应该提到 P1 甚至 P0。

---

## 总结：十个质疑的关联与影响

```
题目歧义                        → 设计决策                → 核心风险
─────────────────────────────────────────────────────────────────
1. 工程价值未定义               → 四维评分体系             → 权重偏差导致推荐错配
2. 智能化梯度不明               → LLM辅助+Agent编排        → 可能未满足自主性期望
3. 持续扫描时态歧义             → 每日批处理               → 可能遗漏小时级时效需求
4. 信息源边界模糊               → 4个有API的源             → 可能遗漏关键社交信源
5. 决策者同质化假设             → 统一日报+多角色          → 单一权重无法同时服务两类人
6. 交互模式未定义               → Push+Push双模式          → 通知频率假设可能不成立
7. 技术路线可操作性差           → RAG对比+趋势检测(P2)      → 最抽象的目标覆盖最弱
8. 4周人力可行性                → 严格scope discipline     → 学习曲线可能超期
9. 评测时间衰减                 → 数学衰减+P2重评分         → 历史评分可能失真
10. 演进路径未定义              → 基础设施预留升级接口      → 功能演进方向可能与实际需求错位
```

所有十个质疑共享一个核心命题：**题目"搭一个雷达"的表述暗示这是一个信息聚合系统，但决策者需要的是一个判断辅助系统——两者在架构复杂度、评测深度和智能化程度上存在量级差异。** 我们选择后者作为设计目标，代价是更高的 LLM 依赖和更复杂的 Agent 架构。如果面试官的原始意图更接近前者（轻量信息聚合），那当前设计存在过度工程化的风险。
