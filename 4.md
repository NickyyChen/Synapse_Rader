# WyRadar 项目 LLM 使用说明

## 1. 使用的模型与工具

| 工具/模型 | 角色 | 使用阶段 |
|----------|------|---------|
| **deepseek-v4-pro[1m]** | 对话引擎，承担所有推理、生成、分析、写作任务 | 全流程 |
| **Claude Code (Anthropic)** | 交互环境，提供文件读写、Web搜索、Bash执行、多轮对话管理 | 全流程 |
| **WebSearch 工具** | 调研：LangGraph/CrewAI/AutoGen 对比；2025年评分框架(ARIA/PEARL/GrandJury)；RAG方案 | 设计调研阶段（三轮搜索） |
| **WebFetch 工具** | 未使用（所有调研通过 WebSearch 完成） | — |
| **Bash (grep/find)** | 文档内搜索：查找疑问句、定位章节标记、统计行数 | 文档修订阶段 |
| **Edit 工具** | 精准段落替换（非全文重写），保持文档上下文一致性 | 文档修订阶段 |
| **Write 工具** | 全文生成：新文档创建或已有文档的完全重写 | 文档生成阶段 |

---

## 2. 各文档的生成与修改明细

### 2.1 design_document.md（PRD，最终 1058 行）

**生成方式：** 主体由 LLM 生成，历经 6 轮重大修改。

| 版本 | 触发 | LLM 做了什么 | 人工改了什么 |
|------|------|------------|------------|
| v1 初稿 | 用户提供 PRD.md + 10 个需求澄清回答 | 生成完整 PRD 初版：单Agent+Tools、Streamlit前端、固定权重(0.35/0.25/0.25/0.15)、Claude API | — |
| v2 权重+架构调整 | 用户要求"商业潜力更高、多智能体、FastAPI、DeepSeek" | 全局重写：单Agent→多Agent(6个)、Streamlit→FastAPI+Jinja2、权重改为0.45、LLM改为DeepSeek | 用户明确指定了4个方向 |
| v3 引入RAG | 用户质疑"为什么不使用RAG" | Web搜索RAG方案→在Analyst模块中集成RAG检索→更新架构图、数据模型、技术栈 | 用户提出质疑方向，LLM搜索并设计具体方案 |
| v4 框架+前端调整 | 用户明确"用LangGraph不用CrewAI、引入Vue 3前端" | CrewAI→LangGraph重写多智能体设计（StateGraph/Node/Edge/AgentState/MemorySaver）；Jinja2→Vue 3+Vite SPA | 用户明确指定技术选型 |
| v5 疑问句消除+搜索增强 | 用户要求"不要出现疑问句、联网搜索三个问题" | 修改3个疑问句标题→陈述句；Web搜索LangGraph对比/RAG方案/评分框架；扩展LangGraph选型分析(加入AutoGen对比)、增强评分体系(自适应权重+置信度+时间衰减) | 用户要求格式修正和搜索方向 |
| v6 PRD结构化 | 用户要求"加入业务背景/功能角色/功能清单/优先级/详细描述，给PM和前后端都能看懂" | 全局重写：技术架构文档→产品需求文档。新增业务背景(§2)、用户角色+场景(§3)、功能全景图(§4,17个功能P0/P1/P2)、每个P0功能的用户故事+UI+API+验收标准(§5)、非功能需求(§6)、附录(§8) | 用户指定PRD结构框架 |

**LLM 自主判断的内容（人工未干预）：**
- 用户故事的具体措辞和场景描述（§3.2 三个场景故事）
- 日报 Markdown 模板的详细格式（§5 F-05）
- 飞书消息模板的 emoji 和排版（§5 F-06）
- Web 页面 ASCII 布局图（§5 F-07/08/09）
- API 请求参数和响应 JSON 的字段定义（§5 F-08）
- 非功能需求的具体指标数值（§6.1 性能目标）
- 术语表词条选择（§8.1）
- 前端组件树的结构设计（§8.4）

---

### 2.2 tech_stack.md（技术栈方案，326 行）

**生成方式：** LLM 一次生成，人工未直接修改内容。

**LLM 的工作：**
- 从 design_document.md 中提取所有技术约束（Python后端、Vue 3前端、LangGraph、DeepSeek、ChromaDB、APScheduler等）
- 为每个技术选型生成：选型理由 + 版本号 + 备选方案对比表 + 被拒绝理由
- 生成 pip requirements.txt 和 package.json 依赖清单
- 设计升级路径总览表（MVP→下一迭代→远期）
- 评估 5 个技术风险 + 缓解措施

**人工的角色：** 发出指令"根据prd文档，写一份完成MVP的最合适的技术栈，保存为tech_stack.md"，之后未修改内容。技术栈中的所有判断（如 Naive UI > Tailwind CSS、ChromaDB 嵌入式 > Qdrant、APScheduler > Celery）由 LLM 自主做出。

**LLM 自主判断的关键点：**
- Naive UI > Tailwind CSS 的判断（基于"组件齐全/开箱即用"标准）
- ChromaDB 嵌入式模式的选型（基于"零外部依赖"原则）
- text-embedding-3-small 和 bge-large-zh-v1.5 的权衡（给出两个选项而非一个）
- APScheduler vs Celery 的判断（基于"MVP 两个 Cron 任务不需要 Redis Broker"）

---

### 2.3 feedback.md（面试回答，266 行）

**生成方式：** LLM 生成，人工修改 1 处（替换 §1.1 关注点）。

**LLM 的工作：**
- 回应面试官 4 个问题的结构化格式
- §1 质疑与假设：从 PRD 中提取 4 个歧义点，按"歧义点→观点→假设→风险"结构展开
- §2 方案设计：从 design_document.md 和 tech_stack.md 中提取架构信息，重绘简化版 ASCII 图
- §3 关键决策：从 PRD 设计过程中反思 6 个决策点，每个含选择/理由/反面/取舍
- §4 LLM使用说明：自述生成和修改历史（初版）

**人工修改的 1 处：**

| 位置 | 旧内容 | 新内容 | 修改原因 |
|------|--------|--------|---------|
| §1.1 | "每周回答"与"每日日报"的冲突 | "值得关注"的判断标准由谁定义——评价体系的本体论风险 | 用户要求去掉"每日/每周"关注点，重新思考更有价值的质疑 |
| §3 决策2 | 日频 vs 周频 | 评价标准的归属——系统预设 vs 用户定义 | 联动 §1.1 的修改，保持质疑与决策的一致性 |
| §4 LLM使用说明 | 提及"每周 vs 每日" | 更新为"谁定义值得关注的标准" | 联动修改 |

**人工指定但 LLM 执行的：**
- 文档长度限制（~5页）→ LLM 控制行数和内容密度
- "所有回答必须有价值"→ LLM 在每个决策点加入了反面分析和取舍说明

---

### 2.4 1.md（十个质疑，独立文件）

**生成方式：** LLM 一次生成，人工未修改内容。

**LLM 的工作：**
- 从题目原文、design_document.md、tech_stack.md 中系统性提取所有可能的歧义点
- 筛选出 10 个有足够深度的质疑（排除过于琐碎或已在 feedback.md 中充分讨论的）
- 每个质疑按"歧义点→我的观点→假设→影响"四段结构展开
- 末尾生成关联总结表，将十个质疑映射到设计决策和核心风险

**LLM 自主判断：** 全部 10 个质疑的选择、排序和展开均由 LLM 自主完成。人工仅发出指令"结合两个文件和面试官问题，给出十个质疑与假设"。

---

### 2.5 2.md（架构图与模块说明，独立文件）

**生成方式：** LLM 一次生成，人工未修改内容。

**LLM 的工作：**
- 从 design_document.md 提取架构信息，重新绘制一张与 PRD 中不同的全景 ASCII 图（增加了 AgentState 细节、并行标注、数据流向、时间线）
- 9 个模块的详细说明表（职责/逻辑/容错/性能/技术），每个模块约 8-10 行
- 数据模型 ER 关系图（ASCII）

**LLM 自主判断：** 架构图的布局、模块的取舍（选 9 个而非 PRD 的 10 个模块）、各模块描述的详细程度。

---

### 2.6 3.md（关键设计决策，独立文件）

**生成方式：** LLM 一次生成，人工未修改内容。

**LLM 的工作：**
- 从 design_document.md 和 feedback.md 的设计决策中扩展：3 个已有决策（Agent架构/评价标准/RAG） + 7 个新增决策（评分机制/MVP边界/技术栈/飞书双渠道/checkpointer/前端简化/冷启动）
- 每个决策按"选择→为什么→被拒绝的方案→取舍代价→关键假设"五段结构
- 末尾生成决策总结表

**LLM 自主判断：** 新增的 7 个决策点由 LLM 自主从 PRD 中识别、提炼并展开论证。人工未指定要写哪些决策或多少条。

---

### 2.7 4.md（本文档，LLM 使用说明）

**生成方式：** LLM 生成，人工未修改内容。

**LLM 的工作：** 回顾全流程对话历史和所有文件生成/修改记录，撰写本自述文档。

---

## 3. LLM 在设计中扮演的四种角色

| 角色 | 说明 | 典型操作 |
|------|------|---------|
| **需求澄清者** | 通过结构化追问将模糊需求转化为具体设计参数 | 初始 10 个追问（信源/评分/频率/交互/存储）；后续"为什么不加RAG""是否有更先进评分标准"等深度追问 |
| **调研助手** | 检索外部知识为设计决策提供依据 | WebSearch：LangGraph vs CrewAI vs AutoGen 三方对比；2025 年评分框架(ARIA/PEARL/GrandJury)；RAG 技术方案 |
| **文档生成器** | 将设计决策转化为结构化文档 | 所有 .md 文件的初版生成，包括表格、ASCII图、代码示例、API格式 |
| **迭代修改器** | 精准修改指定段落，维护文档内一致性 | 权重调整→联动更新评分公式、示例JSON、验收标准；框架更换→联动更新架构图、模块描述、技术栈、代码示例 |

## 4. 人工判断的核心介入点

以下决策由**人工做出**，LLM 执行但未替代判断：

| 决策 | 人工判断 | LLM 贡献 |
|------|---------|---------|
| 使用 Agent 架构 | 决定使用多智能体 | 对比单Agent/多Agent优劣，提供 WebSearch 数据支撑 |
| 选择 LangGraph | 明确指定 | 搜索 LangGraph/CrewAI/AutoGen 对比，提供选型依据 |
| 选择 FastAPI + Vue 3 | 明确指定 | 对比 Streamlit/Flask/Django/React 方案，展开选型理由 |
| 选择 DeepSeek-v4-pro | 明确指定 | 分析上下文窗口/中文能力/成本/兼容性的优势 |
| 集成 RAG | 质疑"为什么不加RAG"→ 决定加入 | 搜索 RAG 方案，设计 ChromaDB + 混合检索的具体集成方式 |
| 评分权重值 | 确认 0.45/0.20/0.20/0.15，商业潜力最高 | 搜索2025评分框架，提出上下文自适应权重和置信度机制 |
| P0/P1/P2 优先级 | 确认 10 个 P0 的 MVP 范围 | 按端到端闭环原则梳理功能依赖，计算工时估算 |
| PRD 结构框架 | 要求"PM和前后端都能看懂"的结构 | 按用户故事+UI+API+验收标准的格式填充每个功能 |

## 5. 工具链全景

```
人工输入 (PRD.md + 10轮需求澄清)
    │
    ▼
deepseek-v4-pro[1m] (推理/生成/分析)
    │
    ├── WebSearch ──→ 外部知识 (框架对比/评分标准/RAG方案)
    │
    ├── Write ──→ design_document.md (6轮迭代)
    │          ──→ tech_stack.md (1次生成)
    │          ──→ feedback.md (1次生成 + 1处人工修改)
    │          ──→ 1.md / 2.md / 3.md / 4.md (1次生成)
    │
    ├── Edit ──→ 精准段落修改 (疑问句→陈述句、权重调整、框架替换)
    │
    └── Bash ──→ grep 查找、wc 统计、find 定位
```

**LLM 没有做的事：**
- 没有代替人工做核心架构决策（Agent选型、框架选型、技术栈选型均由人工指定方向）
- 没有代替人工判断 MVP 优先级（P0/P1/P2 划分由人工确认）
- 没有代替人工做最终质量把关（用户多次要求修改方向，LLM 执行修改）

**人工没有做的事：**
- 没有手写任何一行 Markdown（所有文件由 LLM Write 工具生成）
- 没有手动搜索技术资料（所有调研由 LLM WebSearch 完成）
- 没有手动计算工时或梳理依赖关系（由 LLM 自主分析）
