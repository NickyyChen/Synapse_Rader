# WyRadar 技术情报雷达 — 方案设计反馈

---

## 一、对题目需求描述的质疑与假设

### 1.1 "值得关注"的判断标准由谁定义——评价体系的本体论风险

题目要求系统"识别值得关注的资源"，但从未定义"值得关注"的标准由谁制定、如何验证。这是整个系统中最危险的设计真空。

**具体拆解：**

| 标准来源 | 优势 | 风险 |
|---------|------|------|
| 系统设计者预设（我们的做法：四维评分 + 固定权重） | 快速落地，MVP 可运行 | **设计者的偏见伪装成客观标准。** 商业潜力 0.45 的权重如果与决策者的实际价值判断不一致，推荐会系统性地偏离期望 |
| 决策者显式定义 | 标准与决策者意图一致 | 决策者自己往往说不清自己的判断标准——"看到好的就知道了" |
| 从决策者历史行为中学习 | 反映真实偏好 | 需要冷启动数据和在线学习能力，远超 4 周 MVP 范围 |

**更深层的问题：** "值得关注"对同一团队内的不同角色可能是**完全不同的事物**。技术决策者眼中的"值得关注" = 商业回报高 + 风险可控；技术研究员眼中的"值得关注" = 技术路线新颖 + 可复现 + 有学习价值。一份统一的日报同时服务两类读者，存在内在张力——对决策者是噪声的内容对研究员可能是宝藏。

**我们的缓解措施（不完美但务实）：**
- 四维评分权重向商业潜力倾斜（0.45），优先服务决策者——因为他们是系统的最终客户
- 置信度标注让读者知道"这个分数有多靠谱"，低置信度的高分比高置信度的中分更值得警惕
- 全量列表保留所有评分维度明细，研究员可以自行按性能指标或落地难度重新排序
- P1 迭代中的"用户偏好定制"（F-12）将允许不同角色配置不同的权重分布

**关键假设：** 四维权重的默认值（0.45/0.20/0.20/0.15）与歪麦决策者的真实偏好偏差在可接受范围内。**如果这个假设不成立，系统上线后会出现"高分不跟、低分热点"的错配——这是 PRD 中最大的单点风险。** 建议 MVP 上线第 1 周人工对比：决策者实际决定跟进的项目 vs 系统评出的强烈推荐，计算准确率，若 <70% 则立即调整权重。

### 1.2 "经过验证"的含义模糊

题目要求输出"经过验证的辅助决策报告"，但未定义"验证"的标准。

**我们做了分层处理：**
- MVP（4 周）：LLM 基于公开信息（README、论文摘要、社区数据）做分析 + 置信度标注 + RAG 历史对比。**不进行实际复现实验。** 每条评分附带置信度，低置信度维度标注 ⚠️ 提醒决策者这是 LLM 判断而非实测结论。
- 后续迭代（P2 F-14）：对高分条目自动部署运行 benchmark，验证论文声明的性能指标，实现真正的"工程验证"。

**假设：** MVP 阶段决策者接受 LLM 辅助判断，不要求每条情报都经过代码级复现验证。如果面试官认为"验证"意味着必须实测，那 MVP 范围需要重新评估。

### 1.3 "能力边界"定义不清晰

题目要求"评测关注资源的工程价值的能力边界"，术语在 PRD 中未直接对应。

**我们的解释：** "能力边界" = 该技术能做什么、不能做什么、在什么条件下失效。在 PRD 中体现为：
- Analyst 分析输出中的 `summary_comparison` 字段（与已有技术的对比）
- RAG 历史对比中的"是否是对已有技术的改进/替代"判断
- 评分中落地难度维度（文档完整度、部署成本）间接反映实际可用性边界

**假设：** "能力边界"不要求跑 benchmark 实测，而是基于论文/文档的二次分析。如果面试官的期望是实测能力边界（如压力测试、边缘 case 测试），那需要调整 F-14 的优先级从 P2 提到 P1。

### 1.4 其他隐含问题

| 题目未明确的内容 | 我们的假设 | 风险 |
|-----------------|-----------|------|
| 信源选择标准 | 选择 GitHub/HF/ModelScope/arXiv 四个有公开 API 的信源 | 如果面试官认为应包含 Twitter/Reddit 等社交平台，MVP 范围不足 |
| 评测维度 | 自定四维（商业潜力 0.45/落地 0.20/性能 0.20/兼容性 0.15） | 如果歪麦公司的实际业务方向与权重假设不符，评分体系需重构 |
| 团队技术栈 | 假设 Python 为主 | 若团队为 Java/Go 为主，技术选型需重做 |
| "工程价值"的定义 | 解释为可用性+性能+商业匹配度的综合 | 不同角色对"工程价值"的理解可能完全不同 |

---

## 二、方案设计

### 2.1 系统架构图

```
┌──────────────────────────────────────────────────────────────────────┐
│                          WyRadar 技术情报雷达                          │
│                                                                       │
│  ┌─────────────────────────────────────────────────────────────────┐ │
│  │                    LangGraph 多智能体编排引擎                      │ │
│  │                                                                   │ │
│  │   ┌─────────────┐   ┌─────────────┐   ┌─────────────────────┐   │ │
│  │   │  Collector  │──→│  Curator    │──→│     Analyst          │   │ │
│  │   │  (采集节点)  │   │  (策展节点)  │   │    (分析节点)        │   │ │
│  │   │             │   │             │   │                     │   │ │
│  │   │  GitHub ◄═══╡   │ L1 关键词    │   │ ┌─────────────────┐ │   │ │
│  │   │  HF     ◄═══╡   │ L2 LLM 分类  │   │ │  RAG 检索       │ │   │ │
│  │   │  ModelS.◄═══╡   │ L3 LLM 去噪  │   │ │  (ChromaDB)     │ │   │ │
│  │   │  arXiv  ◄═══╡   │             │   │ │  历史情报→上下文  │ │   │ │
│  │   │  4源并行  │   │  │ 输出:15条/日 │   │ └────────┬────────┘ │   │ │
│  │   └─────────────┘   │             │   │          ▼          │   │ │
│  │                     └─────────────┘   │  4维评分 + 置信度    │   │ │
│  │                                       │  并行分析(N条→8线程) │   │ │
│  │                                       └──────────┬──────────┘   │ │
│  │                                                  │               │ │
│  │                    ┌─────────────────────────────┘               │ │
│  │                    ▼                                              │ │
│  │   ┌─────────────┐   ┌─────────────┐                             │ │
│  │   │  Editor     │──→│ Dispatcher  │                             │ │
│  │   │  (主编节点)  │   │  (分发节点)  │                             │ │
│  │   │             │   │             │                             │ │
│  │   │ LLM 润色    │   │ 飞书Bot @all│                             │ │
│  │   │ Markdown生成│   │ 飞书文档     │                             │ │
│  │   └─────────────┘   └─────────────┘                             │ │
│  │                                                                   │ │
│  │              共享 State（AgentState 贯穿全部节点）                 │ │
│  └─────────────────────────────────────────────────────────────────┘ │
│                                                                       │
│  ┌──────────────────────┐  ┌──────────────────┐  ┌────────────────┐ │
│  │  SQLite + ChromaDB   │  │  FastAPI :8000    │  │  Vue 3 SPA     │ │
│  │  (持久化 + 向量检索)  │  │  (REST API+调度)  │  │  (3页面+NaiveUI)│ │
│  └──────────────────────┘  └──────────────────┘  └────────────────┘ │
│                                                                       │
│  数据流：信源API → Collector(4源并行) → Curator(3级过滤) →            │
│          Analyst(RAG+评分, N条并行) → Editor(LLM润色) →               │
│          Dispatcher(飞书Bot+文档+Web)                                 │
│                                                                       │
│  时间线：7:00自动采集 → 7:00-7:25处理 → 8:00飞书推送                  │
└──────────────────────────────────────────────────────────────────────┘
```

### 2.2 关键模块说明

| 模块 | 一句话职责 | 输入 | 输出 | 核心技术 |
|------|-----------|------|------|---------|
| Collector | 并行采集 4 信源 AI 动态，去重入库 | 4 个 API 端点 | ~60 条 raw_items | httpx async + ThreadPoolExecutor |
| Curator | 三级过滤（关键词→LLM分类→LLM去噪），仅保留 AI Agent/多模态/NLP 相关内容 | ~60 条 raw | ~15 条 curated | Python 规则引擎 + DeepSeek (L2/L3) |
| Analyst | 逐条生成摘要 + 自适应权重四维评分 + RAG 历史对比，系统核心 | ~15 条 curated | ~15 条 analyzed（含评分+置信度+对比） | DeepSeek + ChromaDB 混合检索（向量0.7+BM25 0.3） |
| RAG Engine | 分析前检索历史 top-5 相似情报，注入 LLM 上下文 | title+desc | top-5 相似历史 + 对比要点 | text-embedding-3-small + ChromaDB |
| Editor | 汇总分析结果，LLM 润色生成结构化日报 Markdown | ~15 条 analyzed | 1 份完整日报 MD | DeepSeek (t=0.4) |
| Dispatcher | 双渠道推送：飞书 Bot 群消息(≤10KB, @all) + 飞书文档 | 日报 MD | 推送状态 | lark-oapi |
| FastAPI | REST API 服务 + 手动触发 + 进度轮询 + APScheduler | HTTP 请求 | JSON 响应 | FastAPI + Pydantic v2 + uvicorn |
| Vue 3 SPA | 今日日报 / 历史检索 / 手动触发 三个页面 | API JSON | 渲染页面 | Vue 3 + Vite + Naive UI |
| Scheduler | 每日 7:00 触发采集分析，8:00 触发推送 | Cron 表达式 | LangGraph invoke | APScheduler 内嵌 |

---

## 三、关键设计决策

### 决策 1：Agent 架构 — LangGraph 多智能体

**选择：** LangGraph StateGraph，5 个专业化节点，非单 Agent 也非 CrewAI/AutoGen。

**理由：**
- 单 Agent + 多 Tool 方案：当任务差异大（采集是纯 I/O、分析是 LLM 重推理、推送是 API 调用），单 Agent 的 System Prompt 需要覆盖所有场景，臃肿且调试困难。LangGraph 的每个 Node 独立 Prompt + 独立工具集，职责清晰。
- CrewAI：角色层级模型更偏企业组织模拟，它的线性顺序执行不支持并行 Send API（我们采集 4 源需要并行），状态管理也不透明。
- AutoGen：对话驱动适合代码生成和动态协商，不适合 WyRadar 这样确定的流水线。
- LangGraph 的显式 State：每一步的输入输出通过 AgentState 共享，问题定位只需看 State 快照，这是运维友好性的关键决策。

**取舍：** LangGraph 学习曲线较陡（需理解 DAG 概念），但换来的是生产级的并行、流式、中断恢复能力。1-2 人团队中至少 1 人需要花 2-3 天学习 LangGraph 的核心概念。

### 决策 2：评价标准的归属——系统预设 vs 用户定义

**选择：** 系统预设四维评分标准 + 置信度透明化，MVP 阶段不由用户自定义评价维度。

**理由：**
- 用户往往说不清自己的判断标准。"你觉得什么项目值得跟？""看到了就知道了。"这种隐性知识无法直接转化为系统参数。
- 如果让用户自定义权重，需要提供一个配置界面 + 实时预览 + 调参反馈循环，这在 4 周 MVP 中成本过高。
- 四维评分 + 置信度的组合策略是：系统给出**有把握度的建议**，而非**不容置疑的结论**。决策者看到"商业潜力 5（置信度 90%）"和"商业潜力 5（置信度 50%）"时，对前者的信任度自然高于后者。这让系统保持了"建议者"而非"裁决者"的定位。

**反面——如果让用户定义标准会怎样：**
- 5-8 人团队中技术决策者和技术研究员的"好"标准本就不同（见 §1.1），多套标准意味着多套评分和多份日报，MVP 范围失控
- 用户定义的维度可能缺乏可操作性（如"创新能力"——LLM 难以客观评分）

**取舍：** 系统预设标准在快速落地上有优势，但存在"设计者偏见替代决策者判断"的风险（§1.1 已详述）。缓解措施是 MVP 第 1 周做人工校准对比，若系统推荐与决策者实际判断偏差 >30%，立即调整权重。P1 迭代的个性化配置（F-12）将逐步将标准控制权交还给用户。

### 决策 3：RAG 集成

**选择：** 在 Analyst 节点中集成 RAG，而非独立节点。

**理由（正面）：**
- 解决"历史遗忘"痛点——分析新条目时自动关联历史相似情报
- 去重——检测重复推荐，避免"同一个框架的不同文章连续推荐三天"
- 趋势感知——"这是本月第 3 个多 Agent 框架，与前两个的区别是..."

**理由（反面——如果不加 RAG 会怎样）：**
- 每条情报独立分析，缺少时间维度的对比上下文
- 日报可能出现高度重复的推荐
- 无法追踪同一项目/技术的热度变化轨迹

**取舍：** ChromaDB 增加了运维复杂度（向量索引同步），但它是嵌入式部署，不增加独立容器。首次运行时 ChromaDB 为空，系统降级为空上下文，不影响主流程。

### 决策 4：四维评分 + 上下文自适应权重

**选择：** 商业潜力 0.45（默认）为最高权重，根据情报类型动态调整。

**理由：**
- 商业潜力权重最高：歪麦公司决策者的核心问题是"值不值得投入资源"，商业价值是第一过滤条件。
- 上下文自适应：2025 年 ARIA 框架实验证明，统一权重在约 60% 的场景中会歪曲适用性判断。开源工具的落地难度比商业叙事更值得关注，论文的性能指标权重应更高，产品动态的商业信号应拉满。这些调整由 LLM 先判定情报类型，再自动切换权重分布。
- 置信度机制：LLM 不是神，它可能给出高把握的错判和低把握的正确判断。每个维度标注置信度，让决策者知道"这个 5 分是 LLM 基于充分数据给出的高分"还是"LLM 也不太确定的高分"。

**取舍：** 上下文自适应增加了 Prompt 复杂度。如果 LLM 对情报类型的判断出错，权重分布就会偏。验收标准中设置了类型判断准确率 ≥80% 的门槛。

### 决策 5：MVP 范围——P0 十个功能的边界

**选择：** 10 个 P0 功能覆盖"采集→筛选→分析→RAG→报告→飞书推送→Web 三页面→定时调度"的端到端链路。

**取舍与放弃：**
- 放弃实时推送（P1 F-13）：4 周时间不允许做流式监听，每日批量已满足核心需求。
- 放弃用户偏好定制（P1 F-12）：5-8 人团队 MVP 阶段共看一份日报，个性化在后续迭代做。
- 放弃深度复现验证（P2 F-14）：实测 benchmark 需要 GPU 资源和沙箱环境，MVP 不做。
- 放弃信源扩展（P1 F-11）：Twitter/Reddit/知乎等社交平台大多无官方 API 或需 OAuth，采集复杂度高，4 个有公开 API 的信源已覆盖主要 AI 动态。
- **前端不引入 Vue Router**：MVP 仅 3 个页面，Tab 切换比路由更简单。这减少了一个依赖和概念负担。

**4 周可行性评估：**
按 1-2 人、19-24 天估算，时间紧张但可行。最关键的是 Phase 3（分析+RAG+主编，5-6 天），因为评分 Prompt 的质量直接决定日报价值。建议此处投入最资深的人。

### 决策 6：技术选型 — 全 Python 栈 + 最小外部依赖

**选择：** FastAPI + LangGraph + DeepSeek + SQLite + ChromaDB(嵌入) + Vue 3。

**理由：**
- Python 优先：团队主力语言，LangGraph/ChromaDB/httpx/lark-oapi 均原生 Python。
- SQLite + ChromaDB 嵌入式：零外部依赖，"docker compose up"一条命令即可运行。日均几十条数据，SQLite 完全够用。
- Vue 3 + Naive UI：不引入 Vue Router/Pinia，Table/DatePicker/Modal/Progress 组件齐全，3 个页面快速搭建。
- DeepSeek-v4-pro：1M 上下文 + 中文强 + 便宜 + API 兼容 OpenAI 格式。

**不选的技术：**
- PostgreSQL/Qdrant/Redis/Celery：都需要独立容器，MVP 基础设施过重。
- Streamlit：交互能力不足以支持分页/筛选/实时进度轮询。

---

## 四、LLM 使用说明

### 4.1 使用的模型与工具

| 工具/模型 | 用途 | 使用阶段 |
|----------|------|---------|
| deepseek-v4-pro[1m] | 当前对话的 LLM 引擎 | 全流程 |
| Claude Code (Anthropic) | 交互环境、文件读写、Web 搜索 | 全流程 |
| WebSearch 工具 | 搜索 LangGraph vs CrewAI vs AutoGen 对比、RAG 方案、2025 年评分框架研究 | 设计调研阶段 |

### 4.2 各文档的生成与修改

**design_document.md（PRD）：**

| 内容段落 | 生成方式 | 修改说明 |
|---------|---------|---------|
| §1-2 业务背景与目标 | LLM 根据 PRD.md 原始需求 + 用户多轮对话确认的需求细节，整合生成 | 用户多次纠正：信息来源、评分权重、分发渠道、Agent 框架选型；每次反馈后 LLM 针对性修改 |
| §3 用户角色 | LLM 生成初稿，用户未直接修改 | 角色定义和场景故事基于 5-8 人团队的决策-研究-管理三层分工推导 |
| §4 功能清单 | LLM 根据对话中的需求讨论梳理 17 个功能 | 优先级由需求讨论中的 P0=必须/P1=下迭代/P2=远期 原则划分 |
| §5 每个功能的详细描述 | LLM 生成初版框架（用户故事+描述+UI+API+验收），用户多次迭代细化 | 最大的修改轮次：引入 RAG（用户质疑为何不用 RAG → 搜索确认 → 加入设计）、评分体系从固定权重改为自适应权重（用户质疑是否有更先进标准 → 搜索 ARIA/PEARL/GrandJury → 引入三大增强机制）、Agent 框架从 CrewAI 改为 LangGraph（用户明确要求用 LangGraph） |
| §6 非功能需求 | LLM 基于典型 Web 服务标准生成 | 用户未直接修改，作为产品完整性补充 |
| §8 附录 | LLM 根据设计内容自动生成 | 术语表、ER 图、目录结构、组件树均为 LLM 从设计内容中提取整理 |

**tech_stack.md：**

| 内容段落 | 生成方式 | 修改说明 |
|---------|---------|---------|
| 全部内容 | LLM 根据 PRD 提取技术需求后一次性生成 | 每个技术选型包含选型理由 + 备选方案对比 + 版本号 + 不选的理由。用户要求"写一份完成 MVP 的最合适的技术栈"，LLM 从 PRD 中提取了所有技术约束并展开了详细的选型论证 |

**feedback.md（本文档）：**

| 内容段落 | 生成方式 | 修改说明 |
|---------|---------|---------|
| §1 质疑与假设 | LLM 根据对 PRD 的反身性分析生成 | 核心思路来自对"谁定义值得关注的标准""经验证的含义""能力边界的边界"三个歧义点的批判性追问。LLM 生成初版后人工审视确保每个质疑有价值而非为质疑而质疑 |
| §2 架构图 + 模块 | LLM 从 PRD 中提取核心架构信息，重新绘制简化版 ASCII 图 | 与 PRD 中的详细架构图不同，此图聚焦决策相关信息流，去掉了 ChromaDB/部署等实现细节 |
| §3 关键设计决策 | LLM 反思 PRD 设计过程中的 6 个核心决策点 | 每个决策点都包含：具体选择、正面理由、反面理由（如果不这么做会怎样）、取舍分析。这种结构来自题目要求"解释设计背后的思考、取舍、假设" |
| §4 LLM 使用说明 | LLM 自述 | 记录了使用工具、模型、以及每份文档各段落的生成和修改历史 |

### 4.3 LLM 在设计中扮演的角色

1. **需求澄清者：** 在初始阶段，LLM 通过 10 个追问将模糊的 PRD 需求细化为具体的设计参数（权重/来源/频率/技术栈）。
2. **调研助手：** 通过 WebSearch 检索 LangGraph vs CrewAI vs AutoGen 对比、2025 年评分框架（ARIA/PEARL/GrandJury）、RAG 方案，为设计决策提供外部依据。
3. **文档生成器：** PRD 和 tech_stack 的主体内容由 LLM 生成框架和初稿。
4. **迭代修改器：** 用户每次反馈（改权重/换框架/加 RAG/引入前端）后，LLM 精准修改对应段落，保持全文一致性。

### 4.4 人工判断的介入点

以下决策由人工（对话中的用户）做出，LLM 未替代：

- MVP 的功能优先级划分（哪些进 P0、哪些往后放）
- 评分维度权重的最终数值（0.45/0.20/0.20/0.15，商业潜力最高）
- 技术栈的核心选择（LangGraph 而非 CrewAI、Vue 3 而非 Streamlit、DeepSeek 而非 Claude）
- 4 周可行性判断（1-2 人、19-24 天估算）——LLM 提供估算，人工确认合理性
