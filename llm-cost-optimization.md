# WyRadar LLM 成本优化方案

## 1. 当前状态分析

### 1.1 每日 LLM 调用全景

从 design_document.md 和 tech_stack.md 提取，系统每日有 4 个 LLM 调用场景：

| 场景 | 节点 | 调用次数 | 每次输入 | 每次输出 | 模型 | temperature |
|------|------|---------|---------|---------|------|------------|
| L2 分类 | Curator | 1 次批量（~35 条） | ~7K tokens | ~500 tokens | DeepSeek-v4-pro | 0.1 |
| L3 去噪 | Curator | 1 次批量（~20 条） | ~4K tokens | ~300 tokens | DeepSeek-v4-pro | 0.1 |
| 摘要+评分 | Analyst | 15 次并行（每条约 3000+1000 tokens） | ~45K tokens | ~15K tokens | DeepSeek-v4-pro | 0.2 |
| 日报润色 | Editor | 1 次 | ~10K tokens | ~5K tokens | DeepSeek-v4-pro | 0.4 |
| **合计** | | **~18 次/日** | **~66K tokens** | **~21K tokens** | | |

### 1.2 成本估算（以 DeepSeek 公开定价为参考）

| 模型 | 输入单价 (¥/1M tokens) | 输出单价 (¥/1M tokens) | 日均成本 (¥) | 月均成本 (¥) |
|------|----------------------|----------------------|------------|------------|
| deepseek-chat (v3) | ~1 | ~4 | ~0.15 | ~4.5 |
| deepseek-v4-pro | ~2（估） | ~8（估） | ~0.30 | ~9 |
| GPT-4o (对比) | ~18 | ~72 | ~2.70 | ~81 |
| Claude Opus (对比) | ~22 | ~88 | ~3.30 | ~99 |

**结论：DeepSeek 的绝对成本已经很低（月均 < ¥10），成本优化的主要矛盾不是"省钱"，而是以下三个更实际的问题——**

---

## 2. 成本优化的真正动机

按当前设计，DeepSeek-v4-pro 的日均 LLM 费用约 ¥0.30，月均不到 ¥10。单纯从金额看，优化空间极小。但以下三个问题才是优化的真正驱动力：

| 真正问题 | 说明 |
|---------|------|
| **延迟瓶颈** | 15 条情报串行调用 v4-pro 耗时 2-3 分钟。如果后续信源扩展到 8 个、日均分析 50 条，延迟会线性增长 |
| **速率限制** | DeepSeek API 有 RPM（每分钟请求数）和 TPM（每分钟 token 数）限制。15 条并行分析 + 批量分类可能在峰值触发限流 |
| **模型依赖风险** | 全链路绑定单一模型，如果 v4-pro 服务中断，整个日报生成失败。多模型策略也是容灾策略 |
| **信源扩展后的边际成本** | MVP 仅 4 个信源。P1 扩展 6 个新信源后，日均采集可能从 60 条增至 150 条，分析量从 15 条增至 40 条，成本翻 3 倍 |

---

## 3. 分级模型策略——核心思路

**原则：不是所有 LLM 调用都需要同一个模型。按任务的"认知复杂度"分配模型能力，复杂任务用强模型，简单任务用便宜模型。**

```
任务认知复杂度光谱：

低复杂度                    中复杂度                    高复杂度
├─────────────────────────────┼─────────────────────────────┤
L3 去噪（二分类）              L2 分类（多分类+标签）         Analyst 评分（推理+对比+量化）
Editor 润色（语言转换）                                      RAG 上下文分析
                                                            摘要生成

适合：轻量模型                适合：中等模型                 适合：最强模型
例：DeepSeek-v3-lite         例：DeepSeek-v3               例：DeepSeek-v4-pro
    Qwen-Turbo                   Qwen-Plus                     DeepSeek-R1
    GLM-4-Flash                  Moonshot-v1
```

---

## 4. 具体替换方案

### 4.1 Curator L3 去噪 → 降级到轻量模型

**当前：** DeepSeek-v4-pro, t=0.1, 批量 20 条

**建议：** 替换为 DeepSeek-v3（deepseek-chat）或 Qwen-Turbo

**理由：**
- L3 去噪是简单的二分类任务——判断一条内容是否灌水/营销。输入是 title+description（每条约 200 tokens），输出是 yes/no + 一句话理由（每条约 30 tokens）
- 认知复杂度低，不需要 v4-pro 的推理能力
- 温度 0.1 说明任务对确定性要求高，对创造力要求为零——这正是轻量模型的强项
- 批量 20 条约 4000 input + 600 output tokens，成本差异可忽略，但**释放了 v4-pro 的速率限制配额**

**替换模型候选：**

| 候选 | 优势 | 劣势 |
|------|------|------|
| DeepSeek-v3 (deepseek-chat) | 同 API 端点，仅改 model 参数，零代码改动 | 中文去噪能力待验证 |
| Qwen-Turbo (qwen-turbo) | 极便宜（¥0.3/1M input）、中文理解强 | 需新增 API 接入 |
| GLM-4-Flash (glm-4-flash) | 免费额度、足够应对二分类 | 批量处理稳定性待验证 |

**推荐：DeepSeek-v3**——与 v4-pro 共用同一 API endpoint，仅需修改一行 model 参数，零风险切换。如果效果不降，再考虑切到更便宜的 Qwen-Turbo。

**验证方法：** 取 100 条已由 v4-pro 标注 L3 结果的数据，用 v3 重新标注，计算一致率。一致率 ≥ 95% 即可切换。

---

### 4.2 Curator L2 分类 → 降级到中等模型

**当前：** DeepSeek-v4-pro, t=0.1, 批量 35 条

**建议：** 替换为 DeepSeek-v3 或 Qwen-Plus

**理由：**
- L2 分类是多分类任务：判定领域（AI Agent/多模态/NLP/其他）+ 打二级标签（6 选 1）
- 比 L3 去噪复杂，但远不如 Analyst 评分复杂——它不需要推理"为什么这个框架值得跟"，只需要做语义匹配
- 温度 0.1，对确定性要求高——轻量模型可以胜任

**替换模型候选：**

| 候选 | 优势 | 劣势 |
|------|------|------|
| DeepSeek-v3 | 同 API，零改动；分类能力与 v4 接近 | — |
| Qwen-Plus (qwen-plus) | 性价比高（¥2/1M）、中文标签体系友好 | 需新增 API |
| Moonshot-v1-8k | 长上下文、分类精度好 | 中文标签理解不如 Qwen |

**推荐：DeepSeek-v3**——与 L3 同理，同 API 切换零风险。验证通过后，如追求极致成本可再切 Qwen-Plus。

**验证方法：** 取 100 条已由 v4-pro 分类的数据，用 v3 重新分类。category_l1 一致率 ≥ 90%、category_l2 一致率 ≥ 85% 即可切换。

---

### 4.3 Editor 日报润色 → 降级到轻量模型

**当前：** DeepSeek-v4-pro, t=0.4, 1 次/日

**建议：** 替换为 DeepSeek-v3 或 Qwen-Turbo

**理由：**
- Editor 的任务是"语言润色和衔接过渡"——输入是已经确定的结构化数据（按推荐等级分组的分析结果），输出是加了过渡语和标题的 Markdown
- **Editor 明确不修改评分结果**，这意味着它的输出不需要推理能力，只需要语言组织能力
- 温度 0.4 说明允许一定表达多样性——轻量模型的语言生成能力完全足够
- 这是最安全的降级点：即使润色质量略差，日报结构和数据完整性不受影响

**推荐：DeepSeek-v3 或 Qwen-Turbo**——成本差异极小（1 次/日），但释放 v4-pro 配额。

---

### 4.4 Analyst 评分 → 保持不变，但引入多模型投票

**当前：** DeepSeek-v4-pro, t=0.2, 15 次并行/日

**建议：保持 v4-pro 作为主力，引入轻量模型做交叉验证。**

**理由（为什么 Analyst 不能降级）：**
- Analyst 是系统的价值中枢——如果评分不准，整个日报就失去了意义
- 四维评分需要推理能力：理解技术细节 → 判断商业潜力 → 评估落地难度 → 对比同类技术 → 量化打分。这是整个系统中最复杂的 LLM 任务
- 温度 0.2 和置信度标注说明这个任务对模型能力有硬要求
- **在 Analyst 上省钱是省错地方**

**但可以优化调用方式：**

| 优化方向 | 具体做法 | 预期收益 |
|---------|---------|---------|
| Prompt 压缩 | 将 Few-shot 示例从 3 个减为 2 个（去掉低分示例，低分场景少）；将评分标准说明从完整版压缩为精简版（LLM 记忆评分标准比每次读完整版更省 token） | 每条分析节省 ~800 input tokens，日均节省 ~12K tokens |
| 轻量模型预筛选 | 先用 DeepSeek-v3 做快速初评（只出分数，不写理由），如果初评总分 < 3.0（大概率"暂不跟进"），直接用初评结果；只有 ≥ 3.0 的条目才调用 v4-pro 做完整分析（含理由+对比+置信度） | 假设 15 条中 8 条 < 3.0，节省 8 次 v4-pro 调用 |
| 缓存相似条目 | 如果 RAG 检索到相似度 > 0.95 的历史条目，跳过 LLM 分析，直接复用历史评分并标注"⚡ 评分复用自 {date}" | 日均可能命中 1-2 条 |

**推荐组合：Prompt 压缩（立即可做）+ 轻量模型预筛选（下一迭代）。**

---

## 5. 汇总：推荐的分级模型架构

```
                    采集（无 LLM）
                         │
                         ▼
              ┌─────────────────────┐
              │  Curator L2 (分类)   │  ← DeepSeek-v3 (降级)
              │  ~35条/日, t=0.1    │
              └──────────┬──────────┘
                         │
              ┌──────────▼──────────┐
              │  Curator L3 (去噪)   │  ← DeepSeek-v3 (降级)
              │  ~20条/日, t=0.1    │
              └──────────┬──────────┘
                         │
              ┌──────────▼──────────┐
              │  RAG 检索            │  ← text-embedding-3-small (不变)
              │  ~15次/日            │
              └──────────┬──────────┘
                         │
              ┌──────────▼──────────┐
              │  Analyst 预筛选      │  ← DeepSeek-v3 (新增, 轻量初评)
              │  ~15次/日            │     仅出分数，不写理由
              └──────┬──────┬───────┘
                     │      │
            总分<3.0 │      │ 总分≥3.0
                     │      │
              ┌──────▼─┐  ┌─▼──────────────┐
              │ 直接用  │  │ Analyst 完整分析 │  ← DeepSeek-v4-pro (保留)
              │ 初评结果 │  │ 评分+理由+对比   │     ~7-10次/日
              │ ~5-8条  │  │ +置信度+推荐     │
              └─────────┘  └────────┬────────┘
                                    │
                         ┌──────────▼──────────┐
                         │  Editor 日报润色     │  ← DeepSeek-v3 (降级)
                         │  1次/日, t=0.4      │
                         └─────────────────────┘
```

---

## 6. 成本对比

| 方案 | 模型组合 | 日均 v4-pro 调用 | 日均成本 (¥) | 月均成本 (¥) |
|------|---------|-----------------|------------|------------|
| 当前（全 v4-pro） | v4-pro × 18 次 | 18 次 | ~0.30 | ~9 |
| 降级 Curator + Editor | L2/L3/Editor→v3, Analyst→v4-pro | 15 次 | ~0.22 | ~6.6 |
| 降级 + 预筛选 | 上述 + Analyst 预筛选→v3 | 7-10 次 | ~0.15 | ~4.5 |
| 降级 + 预筛选 + 缓存 | 上述 + 相似条目复用 | 5-8 次 | ~0.12 | ~3.6 |

**绝对金额差异很小（月均省 ¥3-5），但关键收益不在金额：**

| 真正收益 | 说明 |
|---------|------|
| v4-pro 速率限制配额释放 | Analyst 调用次数从 15 降到 7-10，峰值并发压力减半 |
| 单点依赖解除 | L2/L3/Editor 不再依赖 v4-pro，任一下游模型故障不影响其他环节 |
| 延迟改善 | 预筛选 + 缓存使 Analyst 等待时间减少约 30% |
| 信源扩展无忧 | P1 信源从 4→10 后，日均分析量可能增至 40 条，预筛选机制将 v4-pro 调用控制在 15-20 次，不触发限流 |

---

## 7. 实施路线

| 阶段 | 操作 | 风险 | 时间 |
|------|------|------|------|
| 第 1 步 | Curator L2/L3 切换至 DeepSeek-v3（仅改 model 参数），运行 3 天，对比分类/去噪一致率 | 极低——同 API，随时可切回 | MVP 开发期间 |
| 第 2 步 | Editor 切换至 DeepSeek-v3，人工对比 3 天日报润色质量 | 低——不影响数据完整性 | MVP 开发期间 |
| 第 3 步 | Prompt 压缩（Few-shot 减为 2 个 + 评分标准精简），运行 3 天对比评分一致性 | 中——Prompt 改动可能影响评分分布 | MVP 后期或上线第 1 周 |
| 第 4 步 | 实现轻量预筛选（v3 初评 → v4-pro 复评高分段），运行 1 周对比评分一致性 | 中——两阶段评分可能引入不一致 | P1 迭代 |
| 第 5 步 | 实现相似条目缓存复用，运行 1 周验证准确率 | 中——复用阈值需调优 | P1 迭代 |
| 远期 | 评估 Qwen/GLM/Moonshot 作为 Curator 替代模型，进一步降低对 DeepSeek 的单一依赖 | 低——仅 Curator 节点受影响 | P2 |

---

## 8. 一个不建议做的事

**不建议用本地部署的开源模型替代 Analyst 的 v4-pro。**

理由：
- 评分质量是系统的生命线。本地部署的 7B/13B 模型在推理能力和指令跟随上远不如 v4-pro
- 1-2 人团队没有余力维护模型部署、量化、推理优化
- 本地部署需要 GPU 服务器（至少 A100 级别），硬件成本远超 API 调用成本
- 日均 ¥0.30 的 API 费用不值得引入本地部署的运维复杂度

**唯一例外：** 如果歪麦公司已有内部 GPU 集群且处于闲置状态，可以在 P2 阶段评估用本地部署的 Qwen2.5-72B 或 DeepSeek-V3 做 Analyst 降级验证，但不是 MVP 阶段该考虑的事。
